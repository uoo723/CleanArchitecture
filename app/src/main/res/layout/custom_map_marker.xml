<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center">

    <ImageView
        android:id="@+id/speech_bubble"
        android:layout_width="180dp"
        android:layout_height="60dp"
        android:scaleType="fitXY"
        android:src="@drawable/ic_post_marker"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/post_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:textColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="@+id/speech_bubble"
        app:layout_constraintLeft_toLeftOf="@+id/speech_bubble"
        app:layout_constraintRight_toRightOf="@+id/speech_bubble"
        app:layout_constraintTop_toTopOf="@+id/speech_bubble"
        app:layout_constraintVertical_bias="0.3"
        tools:text="blah blah blah blah blah blah blah blah " />

    <ImageView
        android:layout_width="0dp"
        android:layout_height="45dp"
        android:scaleType="centerCrop"
        android:visibility="gone"
        app:layout_constraintLeft_toLeftOf="@+id/speech_bubble"
        app:layout_constraintRight_toRightOf="@+id/speech_bubble"
        app:layout_constraintTop_toTopOf="@+id/speech_bubble"
        tools:ignore="ContentDescription"
        tools:src="@drawable/test_1" />
</android.support.constraint.ConstraintLayout>
